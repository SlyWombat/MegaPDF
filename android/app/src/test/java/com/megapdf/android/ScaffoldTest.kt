package com.megapdf.android

import org.junit.Assert.assertTrue
import org.junit.Test

// Placeholder so :app:testDebugUnitTest exercises the unit-test toolchain from day one.
class ScaffoldTest {
    @Test
    fun `unit test toolchain runs`() {
        assertTrue(true)
    }
}
