# Private test corpus (SDD §5.6)

Drop real-world PDFs in this folder and `CorpusTests` will smoke-test each one:
open → render page 1 → save → reopen.

Contents are **git-ignored** — internal documents must never reach the public
repository. The synthetic fixtures in `SamplePdf.cs` cover contributors.
