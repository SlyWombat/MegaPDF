# Stitch explorations

Generated by Google Stitch from `design.md`, via the Stitch MCP server
(`https://stitch.googleapis.com/mcp`).

- **Project** `projects/12399174274612094497` — "MegaPDF"
- **Design system** `assets/63d9c6c607374e82a46d354d0527d7e6`, created from
  `design.md` with `create_design_system_from_design_md`
- Prompts used the app's real copy and the brand palette from
  `assets/branding/`, not invented content

**These are explorations, not decisions.** They are worth keeping because each
one makes a concrete suggestion about a weakness `design.md` names:

| | Device | Suggestion |
|---|---|---|
| `01-document-view` | desktop | Emphasises **Sign** over the other fifteen actions, rather than the flat row we ship |
| `02-welcome` | desktop | Gives the empty state a job — recents with dates, a drag target, and the "no account, no cloud" promise placed where it sells. The lockup carries the product name as the hero, so no text heading repeats it |
| `03-signature` | desktop | Offers *draw* and *use a photo* with equal weight — which matches how people actually have a signature |
| `04-toolbar-professional` | desktop | Groups the sixteen buttons into Open/Save · Sign · Edit · Tools · history · zoom · view, with labels under icons |
| `05-toolbar-condensed` | desktop | The same grouping with labels dropped — what a narrow window has to fall back to |
| `07-toolbar-professional-logo` | desktop | The grouped toolbar with the brand lockup anchoring the top-left |
| `13-font-text-formatting` | desktop | Face, size and weight for added text (#43) as a contextual bar, not a dialog |
| `10-mobile-editor` | mobile | Four-item bottom bar — Sign, Edit Text, Checkboxes, Save — with the page/zoom pill floating above it |
| `11-mobile-signature` | mobile | The signature sheet at phone width |
| `12-mobile-text-formatting` | mobile | Formatting controls docked above the bottom bar while a text box is selected |
| `09-brand-logo` | — | Stitch's raster of the lockup. **Not usable** — 1:1 with heavy padding and an off-white ground, so it renders as a speck in any toolbar. The screens use inline SVG from `assets/branding/logo.svg` instead |

## The platform split

The desktop design is split in two. Same palette, same lockup; different chrome.

| | Windows (WinUI 3, Fluent) | macOS (AppKit conventions) |
|---|---|---|
| | `windows-welcome.png` | `macos-welcome.png` |
| | `windows-document.png` | `macos-document.png` |
| | `windows-signature.png` | `macos-signature.png` |
| Window controls | caption buttons, top right | traffic lights, top left |
| Title bar | merged into the command bar | unified translucent toolbar |
| Status | bar along the bottom edge | floating page/zoom pill, no bar |
| Dialogs | centred ContentDialog | sheet attached to the window top |
| Radii | 4px control / 8px surface | 6px / 10px |
| Labels | 12px Segoe UI | 11px SF Pro Text |

`docs/design-tokens.md` is the normative version of that table. These renders are
the illustration.

**Logo placement in these renders is not a specification.** It is set in each app
against that platform's conventions; the renders only fix the palette and the fact
that the lockup is the inline SVG.

Two caveats on the Windows set: `windows-document.png` came back in dark theme
with the page itself dark, which is wrong — a PDF page is white paper in both
themes. And a first pass at the Windows variant invented an Acrobat-style ribbon,
a Library/Cloud/Templates sidebar, "MegaPDF Professional" and an engine version
string; the regenerated files here have none of it, but it is a standing reminder
that generated screens drift toward the category average and have to be read
before they are trusted.

**The logo is inline SVG, never an image.** Stitch's first pass placed a
generated raster in the toolbar; because that raster is square and mostly
padding, an `h-8` slot showed a ~14px speck. Every screen now pastes
`assets/branding/logo.svg` inline and sizes it by height with `width:auto`.

**Where they diverge from the product, the product is right:**

- Ticks are drawn as blue check marks. Ours are **✗ in `#202020`** by default —
  SDD Appendix B #3, and a §6.2 contract across four platforms.
- The "Accessibility" and "Support" links in the status bar do not exist.
- The document content is invented sample text; ours comes from the fixtures.

Regenerate or extend with the Stitch MCP server. `design.md` is the input — keep
it current and the explorations stay grounded.
