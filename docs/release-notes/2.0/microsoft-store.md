# Microsoft Store — "What's new in this version", 2.0.0.0

Partner Center → the submission → **Store listings** → *What's new in this
version*, per language. Limit 1500 characters; the count is beside each block.
Paste the English into both `en-US` and `en-CA`.

*The French is signed off: a francophone reviewed it (Dave, 2026-09-18), and the Fable review's recommendations are applied (#242) — see [README](README.md).*

## English — `en-US` and `en-CA`

**What's new** [1500] (1483)

```
Fix the document's own text, far more often
MegaPDF used to refuse a correction when it could not promise the rest of the page would stay put. It now makes those changes. Over a 22,000-edit test run, every one of the 112 edits it used to refuse goes through, and none of them disturbs the page.

Redact
Mark text or a picture and MegaPDF removes it from the file. Whiteout only covers, and now says so.

It tells you what it is doing
Opening, saving, checking the saved file, searching, checking a page, making a smaller copy, preparing to print: each says so while it works. Anything quicker than half a second still says nothing at all.

Your work is safer
Closing, quitting or opening another file with unsaved changes always asks first. A save that fails can no longer leave the original empty. Changes made while a save is running are no longer marked as saved. Printing no longer freezes the window.

Protected PDFs
Open one with its password. If its owner restricted what may be changed, MegaPDF respects that and says so, and the owner password unlocks it. You can also set a password on a document, change it, or remove it.

Very large PDFs
A 2.5 GB file used to take a minute to open and gigabytes of memory. It now opens straight away. Banner-size pages measure at their real size, and pages full of pictures draw several times faster.

One row of tools
The toolbar is a single row, with the pickers appearing beside the tool they belong to and everything else under More.
```

## Français (Canada) — `fr-CA`

**Quoi de neuf** [1500] (1485)

```
Corriger le texte du document, bien plus souvent
MegaPDF refusait une correction lorsqu'il ne pouvait pas garantir que le reste de la page resterait en place. Il l'effectue maintenant : sur 22 000 modifications d'essai, les 112 qu'il refusait passent toutes, sans perturber la page.

Caviarder
Marquez du texte ou une image et MegaPDF le retire du fichier, pas seulement de l'affichage. Masquer ne fait que recouvrir, et le dit maintenant.

Il vous dit ce qu'il fait
Ouverture, enregistrement, vérification du fichier enregistré, recherche, vérification d'une page, copie réduite, préparation de l'impression : chaque tâche s'annonce pendant qu'elle travaille.

Votre travail est mieux protégé
Fermer, quitter ou ouvrir un autre fichier avec des modifications non enregistrées pose toujours la question d'abord. Un enregistrement raté ne peut plus vider l'original. L'impression ne fige plus la fenêtre.

PDF protégés
Ouvrez-en un avec son mot de passe. Si son propriétaire a restreint les modifications, MegaPDF le respecte et vous le dit. Définissez un mot de passe sur un document, changez-le ou retirez-le.

PDF très volumineux
Un fichier de 2,5 Go prenait une minute à ouvrir et des gigaoctets de mémoire. Il s'ouvre maintenant tout de suite. Les pages pleines d'images s'affichent plusieurs fois plus vite.

Une seule rangée d'outils
La barre d'outils tient sur une rangée : les sélecteurs apparaissent à côté de l'outil auquel ils appartiennent, et le reste se trouve sous Plus.
```

## Français (France) — `fr-FR`

**Quoi de neuf** [1500] (1485)

```
Corriger le texte du document, bien plus souvent
MegaPDF refusait une correction lorsqu'il ne pouvait pas garantir que le reste de la page resterait en place. Il l'effectue maintenant : sur 22 000 modifications d'essai, les 112 qu'il refusait passent toutes, sans perturber la page.

Caviarder
Marquez du texte ou une image et MegaPDF le retire du fichier, pas seulement de l'affichage. Masquer ne fait que recouvrir, et le dit maintenant.

Il vous dit ce qu'il fait
Ouverture, enregistrement, vérification du fichier enregistré, recherche, vérification d'une page, copie réduite, préparation de l'impression : chaque tâche s'annonce pendant qu'elle travaille.

Votre travail est mieux protégé
Fermer, quitter ou ouvrir un autre fichier avec des modifications non enregistrées pose toujours la question d'abord. Un enregistrement raté ne peut plus vider l'original. L'impression ne fige plus la fenêtre.

PDF protégés
Ouvrez-en un avec son mot de passe. Si son propriétaire a restreint les modifications, MegaPDF le respecte et vous le dit. Définissez un mot de passe sur un document, changez-le ou retirez-le.

PDF très volumineux
Un fichier de 2,5 Go prenait une minute à ouvrir et des gigaoctets de mémoire. Il s'ouvre maintenant tout de suite. Les pages pleines d'images s'affichent plusieurs fois plus vite.

Une seule rangée d'outils
La barre d'outils tient sur une rangée : les sélecteurs apparaissent à côté de l'outil auquel ils appartiennent, et le reste se trouve sous Plus.
```

> The two French blocks differ only where the derivation rules say they should.
> Neither uses *courriel*, *infonuagique*, *crochet* or *fin de semaine*, and the
> only punctuation the France rule would change is `:` — which Quebec spaces the
> same way. So they are identical here by construction, not by oversight.
